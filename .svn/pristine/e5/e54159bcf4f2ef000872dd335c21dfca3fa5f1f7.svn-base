package course.command;

import command.WriteCommand;
import course.CourseModifier;

/**
 * Command to update the name of a course
 */
public abstract class UpdateCourseName implements WriteCommand<CourseModifier> {
}
