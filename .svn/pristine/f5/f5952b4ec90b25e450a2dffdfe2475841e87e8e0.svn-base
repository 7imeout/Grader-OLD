package assignment.command;

import command.WriteCommand;

/**
 * Encapsulates all operations that modify (update) assignment categories
 * in the Grader program.
 */
public abstract class UpdateAssignmentCategory implements WriteCommand {
}
