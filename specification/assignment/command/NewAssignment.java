package assignment.command;

import command.WriteCommand;
import course.CourseModifier;

/**
 * NewAssignment is created when the user adds a new assignment to the course. 
 *
 */

public abstract class NewAssignment implements WriteCommand<CourseModifier> {
}
